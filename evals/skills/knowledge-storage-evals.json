{
  "skill_name": "knowledge-storage",
  "evals": [
    {
      "id": 1,
      "prompt": "We just finished our Q4 planning meeting. Here are the key decisions: 1) Focus on enterprise segment, 2) Hire 3 more engineers, 3) Launch beta by Nov 15. Can you save these meeting notes?",
      "expected_output": "The skill should search for existing Q4 planning documents, propose updating if found or creating new if not. Must show a proposal before executing, then confirm with a native URL (not kgent:// URI).",
      "files": [],
      "expectations": [
        "The skill runs 'kgent search' before proposing any write",
        "The skill presents a proposal to the user and waits for approval before executing",
        "The proposal includes a title derived from the meeting content",
        "The content is formatted as clean markdown with structured headings/lists",
        "The final confirmation message includes a native platform URL (https://...), not a kgent:// URI",
        "The skill shows the op_id so the user can undo if needed",
        "The proposal shows provenance: where the target backend came from (explicit/preferences/config default)",
        "The skill runs 'kgent route --dry-run' before executing any write",
        "The skill wraps the write with 'kgent journal begin' and 'kgent journal end'",
        "For Lark targets the skill delegates execution via lark-integration, not 'kgent update'"
      ]
    },
    {
      "id": 2,
      "prompt": "Update the「部门新人入职」doc in ForDev产品部门 — the 了解业务 section has a leftover test sentence （我改了文档，改了个文档，改了啊呀） — remove that sentence. The rest of the doc stays exactly the same.",
      "expected_output": "Find the real 部门新人入职 doc via search, propose UPDATE of it, execute through lark-integration with journal begin/end, confirm with native URL.",
      "files": [],
      "expectations": [
        "The skill searches for 'deployment guide' before proposing anything",
        "The skill proposes UPDATE of an existing document, not CREATE",
        "The proposal mentions the existing document title and URI",
        "After approval, the update executes via the platform path: Lark targets delegate through lark-integration; non-integration backends (kgent hosted backend) call 'kgent update' with the existing doc URI",
        "The confirmation message includes a native platform URL, not a kgent:// URI",
        "The provenance records intent=update because a matching doc was found",
        "The skill runs 'kgent route --dry-run' before executing any write",
        "The skill wraps the write with 'kgent journal begin' and 'kgent journal end'",
        "For Lark targets the skill delegates execution via lark-integration, not 'kgent update'"
      ]
    },
    {
      "id": 3,
      "prompt": "Save this to DingTalk please: 'Partner API access tokens should be rotated every 30 days. Contact security@company.com for requests.'",
      "expected_output": "The skill should target dingtalk (explicit user input), search for existing content via dingtalk-integration, and propose create or update accordingly. Must show provenance noting 'explicit user input' as the backend source.",
      "files": [],
      "expectations": [
        "The skill targets dingtalk backend (explicit user input overrides any defaults)",
        "The provenance records target source as 'explicit user input'",
        "The skill searches dingtalk for existing content before proposing",
        "A proposal is shown before any write",
        "The confirmation includes a native DingTalk URL format, not kgent://dingtalk/...",
        "The content is formatted as markdown, not raw text",
        "The skill runs 'kgent route --dry-run' before executing any write",
        "The skill wraps the write with 'kgent journal begin' and 'kgent journal end'",
        "For DingTalk targets the skill delegates execution via dingtalk-integration, not 'kgent update'"
      ]
    },
    {
      "id": 4,
      "prompt": "I wrote a new onboarding checklist. Save it to both Lark and DingTalk so both teams can access it.",
      "expected_output": "The skill should create the document on both backends (multi-backend fan-out). Should show a proposal listing both targets, get confirmation, execute both through their integration skills, and report both native URLs.",
      "files": [],
      "expectations": [
        "The proposal lists both lark and dingtalk as targets",
        "The skill searches both backends for existing content before proposing",
        "The user is asked to confirm before any write",
        "Both backends receive the write: Lark targets delegate through lark-integration; DingTalk targets delegate through dingtalk-integration",
        "The confirmation message includes native URLs for both backends",
        "If one backend fails, the other still succeeds (partial success reporting)",
        "The op_id is reported for undo capability",
        "The skill runs 'kgent route --dry-run' before executing any write",
        "The skill wraps the write with 'kgent journal begin' and 'kgent journal end'"
      ]
    },
    {
      "id": 5,
      "prompt": "Remember that the API rate limit is 1000 requests per minute per client. Save it.",
      "expected_output": "The skill should extract 'API rate limit' as the title, the policy as content, search for existing docs, and propose create/update with clean markdown formatting.",
      "files": [],
      "expectations": [
        "The skill derives a title from the content (e.g., 'API Rate Limit')",
        "The content is formatted as markdown (not just the raw sentence)",
        "The skill searches for existing docs about API rate limits",
        "A proposal is shown before write",
        "Native URL in confirmation, not kgent:// URI",
        "The skill handles the implicit 'save it' intent without needing explicit backend specification",
        "The skill runs 'kgent route --dry-run' before executing any write",
        "The skill wraps the write with 'kgent journal begin' and 'kgent journal end'",
        "For Lark targets the skill delegates execution via lark-integration, not 'kgent update'"
      ]
    },
    {
      "id": 6,
      "prompt": "We have a confidential document about the upcoming acquisition. Save it to our knowledge base.",
      "expected_output": "The skill should recognize the sensitive content and either propose routing to internal-zone backends only, or warn that external backends (dingtalk, wecom) cannot accept confidential content.",
      "files": [],
      "expectations": [
        "The skill recognizes 'confidential' as a sensitivity signal",
        "The skill does NOT propose routing to external-zone backends (dingtalk/wecom) without warning",
        "If proposing an external backend, the skill warns about the sensitivity zone restriction",
        "The proposal mentions the sensitivity classification",
        "The skill asks the user to confirm the target backend given the sensitivity",
        "The skill runs 'kgent route --dry-run' before executing any write",
        "The skill wraps the write with 'kgent journal begin' and 'kgent journal end'",
        "For Lark targets the skill delegates execution via lark-integration, not 'kgent update'"
      ]
    },
    {
      "id": 7,
      "prompt": "Add these notes to the Password Rotation Policy doc.",
      "expected_output": "The skill should search for 'Password Rotation Policy' (密码轮换策略), find the existing wiki doc, and propose UPDATE. If multiple matches exist (e.g., on different backends), offer per-copy options. Fixture note: the target must be a doc that actually exists in the tenant — an earlier 'API Guidelines' premise here pointed at a nonexistent doc, driving the agent into the CREATE path and leaving a stray eval-created doc in the tenant (cleaned up 2026-09-07).",
      "files": [],
      "expectations": [
        "The skill searches for 'Password Rotation Policy' before proposing anything",
        "The skill proposes UPDATE (not CREATE) since a matching doc exists",
        "If multiple matches exist, the skill offers per-copy options (update all / update specific / create new)",
        "The proposal clearly names the existing document(s) found",
        "The user is asked to choose which copy to update when multiple exist",
        "The skill runs 'kgent route --dry-run' before executing any write",
        "The skill wraps the write with 'kgent journal begin' and 'kgent journal end'",
        "For Lark targets the skill delegates execution via lark-integration, not 'kgent update'"
      ]
    },
    {
      "id": 8,
      "prompt": "Add our deployment runbook to the Engineering Wiki. We use progressive rollouts with a 5% canary.",
      "expected_output": "The skill resolves the wiki intent, lists knowledge spaces, inspects the space structure, and proposes creating a wiki node under a topically-fitting parent (the parent token comes from the space listing — never guessed, S83/N22). The create executes via the platform path: Lark targets delegate the wiki-node create through lark-integration (same space and parent tokens); non-integration backends (kgent hosted backend) create with 'kgent create --wiki-space <space_id> --parent-node-token <token>'. Confirms with a /wiki/ native URL.",
      "files": [],
      "expectations": [
        "The skill recognizes the wiki intent and targets a knowledge space",
        "The skill lists knowledge spaces with 'kgent wiki spaces list'",
        "The skill inspects the space structure to find a fitting parent (top-level nodes)",
        "The parent token passed to --parent-node-token matches a token from the space listing — never guessed (S83, N22)",
        "The create carries the resolved space and parent tokens: for non-integration backends (kgent hosted backend) the 'kgent create' command passes '--wiki-space' and '--parent-node-token'; for Lark targets the wiki-node create delegates through lark-integration with the same tokens",
        "The confirmation message uses a native URL with '/wiki/' path, not '/docx/'",
        "No kgent:// URI appears in the confirmation",
        "The skill runs 'kgent route --dry-run' before executing any write",
        "The skill wraps the write with 'kgent journal begin' and 'kgent journal end'",
        "For Lark targets the skill delegates execution via lark-integration, not 'kgent update'"
      ]
    },
    {
      "id": 9,
      "prompt": "Save these Q3 security review notes.",
      "expected_output": "No wiki/doc mention, no existing match, no sibling topic — with all resolution factors equal the skill asks the user wiki node vs flat doc BEFORE proposing ([a] wiki node / [b] flat doc, S84), then builds the proposal for the chosen type and records the decision source in provenance.",
      "files": [],
      "expectations": [
        "The skill runs the update-first search first",
        "When no factor determines the target type, the skill asks the user wiki vs flat doc (offers both options)",
        "The skill does NOT silently pick a target type",
        "The proposal records the user's choice and provenance notes the choice source ('user choice')",
        "The chosen target type determines the create command shape (--wiki-space vs plain create)",
        "The skill runs 'kgent route --dry-run' before executing any write",
        "The skill wraps the write with 'kgent journal begin' and 'kgent journal end'",
        "For Lark targets the skill delegates execution via lark-integration, not 'kgent update'"
      ]
    },
    {
      "id": 10,
      "prompt": "Save this to WeCom please: 'Customer escalation hotline is +86-400-123-4567, staffed 9:00-18:00 CST. Escalate SEV1 incidents within 15 minutes.'",
      "expected_output": "The skill should target wecom (explicit user input), search for existing content via wecom-integration, and propose create or update accordingly. Must show provenance noting 'explicit user input' as the backend source.",
      "files": [],
      "expectations": [
        "The skill targets wecom backend (explicit user input overrides any defaults)",
        "The provenance records target source as 'explicit user input'",
        "The skill searches wecom for existing content before proposing",
        "A proposal is shown before any write",
        "The confirmation includes the native WeCom doc URL from the platform response, not kgent://wecom/...",
        "The content is formatted as markdown, not raw text",
        "The skill runs 'kgent route --dry-run' before executing any write",
        "The skill wraps the write with 'kgent journal begin' and 'kgent journal end'",
        "For WeCom targets the skill delegates execution via wecom-integration, not 'kgent update'"
      ]
    }
  ]
}